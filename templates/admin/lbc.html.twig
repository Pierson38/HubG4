{{ include('partials/main.html.twig') }}

    <head>
        
        {{ include('partials/title-meta.html.twig', {title: 'Admin - Liste des annonces lbc'}) }}

        {{ include('partials/head-css.html.twig') }}

    </head>

    {{ include('partials/body.html.twig') }}

        <!-- Begin page -->
        <div id="layout-wrapper">

            {{ include('partials/menu.html.twig') }}

            <!-- ============================================================== -->
            <!-- Start right Content here -->
            <!-- ============================================================== -->
            <div class="main-content">

                <div class="page-content">
                    <div class="container-fluid">

						{{ include('partials/page-title.html.twig', {pagetitle: 'Admin', title: 'Lbc'}) }}

                        <div class="row">
                            <div class="col-lg-12">
                                <div class="card">
                                    <div class="card-body">
                                        <div class="table-responsive">
                                            <table class="table align-middle table-nowrap table-hover">
                                                <thead class="table-light">
                                                    <tr>
                                                        <th scope="col">Titre</th>
                                                        <th scope="col">Créer par</th>
                                                        <th scope="col">Prix €</th>
                                                        <th scope="col">Créer le</th>
                                                        <th scope="col">Categorie</th>
                                                        <th scope="col">Action</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    {% for lbc in lbcs %}
                                                    <tr>
                                                        <td>
                                                            <h5 class="font-size-14 mb-1"><a href="javascript: void(0);" class="text-dark">{{lbc.title}}</a></h5>
                                                        </td>
                                                        <td>
                                                            {{lbc.createdBy}}
                                                        </td>
                                                        <td>{{lbc.price}} €</td>
                                                        <td>{{lbc.createdAt|format_datetime('short', 'short', locale="fr")}}</td>
                                                        <td>{{lbc.category ? lbc.category : "Pas de categorie"}}</td>
                                                        <td>
                                                            <ul class="list-inline font-size-20 contact-links mb-0">
                                                                <li class="list-inline-item px-2">
                                                                    <a href="{{path("app_lbc_detail", {id: lbc.id})}}" title="Voir"><i class="bx bx-search"></i></a>
                                                                </li>
                                                                <li class="list-inline-item px-2">
                                                                    <a href="{{path("app_lbc_delete", {id: lbc.id})}}" title="Delete"><i class="bx bx-trash"></i></a>
                                                                </li>
                                                               {#  <li class="list-inline-item px-2">
                                                                    <a href="{{path("app_admin_promos_edit", {id: promo.id})}}" title="Edit"><i class="bx bx-pencil"></i></a>
                                                                </li>
                                                                <li class="list-inline-item px-2">
                                                                    <a href="{{path("app_admin_promos_delete", {id: promo.id})}}" title="Delete"><i class="bx bx-trash"></i></a>
                                                                </li> #}
                                                            </ul>
                                                        </td>
                                                    </tr>
                                                    {% endfor %}
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div> <!-- container-fluid -->
                </div>
                <!-- End Page-content -->

                
                {{ include('partials/footer.html.twig') }}
            </div>
            <!-- end main content-->

        </div>
        <!-- END layout-wrapper -->

        {{ include('partials/right-sidebar.html.twig') }}

        {{ include('partials/vendor-scripts.html.twig') }}
        
        <!-- App js -->
        <script src="/js/app.js"></script>

    </body>
</html>
