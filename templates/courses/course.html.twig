{{ include('partials/main.html.twig') }}

<head>

	{{ include('partials/title-meta.html.twig', {title: 'Cours - ' ~ course.title}) }}

	{{ include('partials/head-css.html.twig') }}

</head>

{{ include('partials/body.html.twig') }}

<!-- Begin page -->
<div id="layout-wrapper">

	{{ include('partials/menu.html.twig') }}

	<!-- ============================================================== -->
	<!-- Start right Content here -->
	<!-- ============================================================== -->
		<div class="main-content"> <div class="page-content">
			<div
				class="container-fluid">

				{# {{ include('partials/page-title.html.twig', {pagetitle: 'Cours', title: course.title}) }} #}

				<div class="row">
					<div class="col-lg-12">
						<div class="card">
							<div class="card-body">

								<div class="course-container">
									<h1>{{course.title}}</h1>


									<p>
										{% for tag in course.tags %}
											<a href="javascript: void(0);" class="badge badge-soft-primary font-size-11 m-1">{{tag}}</a>
										{% endfor %}
									</p>

								</div>

								<br>

								<p>Intervenants :
									{{course.professor.firstname ~ " " ~ course.professor.lastname}}</p>

								<p>Salle :
									{{course.classroom}}</p>

								<p>Description :</p>
								<p>{{course.description}}</p>
								<p>De
									{{course.startAt|format_datetime('short', 'short', locale="fr")}}
									à
									{{course.endAt|format_datetime('short', 'short', locale="fr")}}</p>


								<br>

								<h3>Etudiants</h3>
								                                                                                                {# <div>
																																	{% for student in course.promo.users.values %}
																																		<div class="d-flex justify-content-between">
																																			<p>{{student.firstname ~ " " ~ student.lastname}}</p>
																																			<div>
																																				<a class="font-size-20" href="{{path('app_messengers_create_converstation_user', {id: student.id})}}">
																																					<i class="bx bx-message-square-dots"></i>
																																				</a>
																																			</div>
																																		</div>
																																	{% endfor %}
																																</div>  #}
								{# Faire Fichier #}

								<br>

								<div class="table-responsive">
									<table class="table align-middle table-nowrap table-hover">
										<thead class="table-light">
											<tr>
												<th scope="col" style="font-weight: bold">Prénom</th>
												<th scope="col" style="font-weight: bold">Nom</th>
												<th scope="col" style="font-weight: bold">Message</th>
											</tr>
										</thead>

										<tbody>

											{% for student in course.promo.users.values %}
												<tr>
													<td>{{student.firstname}}</td>
													<td>{{student.lastname}}</td>
													<td>
														
																<a class="font-size-20" href="{{path('app_messengers_create_converstation_user', {id: student.id})}}">
																	<i class="bx bx-message-square-dots"></i>
																</a>
															
													</td>
												</tr>
											{% endfor %}

										</tbody>

									</table>
								</div>

							</div>
						</div>
					</div>
				</div>


			</div>
			<!-- container-fluid -->
		</div>


		<!-- End Page-content -->


		{{ include('partials/footer.html.twig') }}
	</div>

	<!-- end main content-->

</div>
<!-- END layout-wrapper -->

{{ include('partials/right-sidebar.html.twig') }}

{{ include('partials/vendor-scripts.html.twig') }}

<!-- Script -->
<style>
	.course-container {
		display: flex;
		align-items: center;
	}

	h1 {
		margin-right: 20px;
	}
</style>

<!-- App js -->
<script src="/js/app.js"></script></body></html>
